/**
 * Page breaks module entry point.
 * Provides functionality for WYSIWYG pagination.
 */

import { updatePagination, setupPaginationListeners } from './pageBreaksCore.js';

// Re-export the main functions
export {
    updatePagination,
    setupPaginationListeners
};
